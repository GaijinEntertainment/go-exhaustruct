//line generated.y:1

package testdata

// A generator that emits its //line ahead of the package clause remaps the
// file's own position, so the name a file is keyed under differs from the name
// it is read from.

//exhaustruct:optional
var lineBeforePackageVar int
