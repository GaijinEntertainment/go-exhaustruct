package testdata

// This file carries a //line directive, as code generators emit. Positions past
// it name a virtual file that does not exist on disk, so anything that reads
// the file must use the physical position instead.

//line virtual.y:100

//exhaustruct:optional
var linedVar int
